services:
  - type: web
    name: odoo-implementation-platform
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build:css
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000