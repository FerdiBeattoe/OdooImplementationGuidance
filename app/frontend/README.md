# Frontend

This is a static implementation-control shell.

Run it from `app/frontend` with any local static server, for example:

```powershell
python -m http.server 4174
```

Then open `http://127.0.0.1:4174/`.
